{
  "name": "@project-booth/backend",
  "version": "0.0.0",
  "private": true,
  "description": "Project Booth API and worker runtime",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "build": "tsc -b --pretty false",
    "db:migrate": "pnpm run build && node dist/src/migrate-main.js up",
    "db:status": "pnpm run build && node dist/src/migrate-main.js status",
    "health:datastores": "node dist/src/datastore-health-main.js",
    "start:api": "node dist/src/main.js",
    "start:worker": "node dist/src/worker-main.js",
    "test": "pnpm run build && node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@project-booth/config": "workspace:*",
    "@project-booth/domain": "workspace:*",
    "@project-booth/game-engine": "workspace:*",
    "fastify": "5.10.0",
    "iovalkey": "0.3.3",
    "pg": "8.22.0",
    "pino": "10.3.1"
  },
  "devDependencies": {
    "@types/pg": "8.20.0"
  }
}
