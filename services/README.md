# Services

Deployable backend processes live here.

The `backend/` workspace will begin in M1.2 with the direct Fastify API process. The worker entry point follows in M1.3.

M1.1 intentionally contains no runtime service code.
