# Cross-package tests

Integration, concurrency, load, and end-to-end suites live here when they span multiple workspace packages.

Focused unit tests remain beside the package they verify. M1.1 intentionally adds no test fixtures.
